# Disney MCP Server Configuration

# Logging level (DEBUG, INFO, WARN, ERROR)
MOUSE_MCP_LOG_LEVEL=INFO

# Database path (default: .data/disney.db in project root)
# MOUSE_MCP_DB_PATH=/custom/path/disney.db

# Session refresh buffer in minutes (default: 60)
# Refresh sessions this many minutes before they expire
MOUSE_MCP_REFRESH_BUFFER=60

# Request timeout in milliseconds (default: 30000)
MOUSE_MCP_TIMEOUT=30000

# Browser options for Playwright
# Set to "true" to show browser window (useful for debugging)
MOUSE_MCP_SHOW_BROWSER=false

# =============================================================================
# Embedding Configuration
# =============================================================================

# Embedding provider selection (auto, openai, transformers)
# - auto: Use OpenAI if API key available, otherwise Transformers.js (default)
# - openai: Force OpenAI embeddings (requires OPENAI_API_KEY)
# - transformers: Force local Transformers.js embeddings (no API key needed)
MOUSE_MCP_EMBEDDING_PROVIDER=auto

# OpenAI API key for embeddings (optional)
# Required if MOUSE_MCP_EMBEDDING_PROVIDER=openai
# Used automatically if MOUSE_MCP_EMBEDDING_PROVIDER=auto and key is set
# OPENAI_API_KEY=sk-...

# =============================================================================
# Environment
# =============================================================================

# Node environment (development, production, test)
# Affects default log level: DEBUG in development, INFO in production
NODE_ENV=development
